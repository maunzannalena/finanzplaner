# Copy to .env.local (locally) or set as environment variables on Vercel.
# Without these two variables the app runs on in-memory demo data.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
